{
  "name": "concourse",
  "version": "0.1.0",
  "private": true,
  "description": "Local Node host + single-file HTML bridge that lets non-developers drive a Claude Code agent loop without a terminal. See SPEC-concourse-bridge.md.",
  "type": "module",
  "main": "host.mjs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node host.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
