# BUILD-LEDGER — Concourse

One row per completed unit of work (CLAUDE.md Rule 3).

| Date | Unit | Files touched | Acceptance criteria met |
|---|---|---|---|
| 2026-07-31 | Copilot PR #17 — rail doesn't go idle mid-concurrent-work | host.mjs, test/reducer.test.mjs | Copilot flagged an edge case in the concurrent-tool patch: when the current (last-started) tool's TOOL_END arrived before an earlier concurrent tool's, the reducer reset the rail to `thinking` ("Working out what to do next") and cleared `currentToolUseId` while a read was still in flight — a legibility lie. Fix: each `inFlight` entry now stores its rail snapshot (state/activity/friendly/truePath); when the current tool ends with others still in flight, the rail re-points to the most-recently-started remaining tool instead of going idle, and only returns to `thinking` when nothing is left. `filesTouched` unchanged (still records only the file fields). +1 test (current-ends-first ordering). Stays single-activity; showing N at once ("Reading 2 files") remains the v0.2 rail work. 60 green. |
| 2026-07-31 | E2E fix — redact absolute paths outside the workspace | host.mjs, test/normaliser.test.mjs | Found while poking the running app: a tool result reporting a write OUTSIDE the workspace (the CLI, inheriting the user's ~/.claude, wrote a memory file to `C:\Users\…\.claude\projects\…\memory\…`) leaked the full absolute path into the raw detail — `redactWorkspacePaths` only stripped the workspace root. Now `redactWorkspacePaths` also calls `collapseAbsolutePaths`, which reduces any remaining absolute path (Windows drive / UNC / multi-segment POSIX) to just its file name, leaving URLs and workspace-relative paths untouched (CLAUDE.md: absolute paths never reach the UI, not even behind the closed disclosure). +2 tests; live-verified the raw `…\project_v0.2_requirements.md` detail now renders as just `project_v0.2_requirements.md`. Note for v0.2: the sub-agent writing memory OUTSIDE the workspace is a scoping question (no `--bare`, so it inherits ~/.claude) — display leak fixed here; the out-of-workspace write itself is worth revisiting. |
| 2026-07-31 | E2E — §13 acceptance pass (Windows) | (verification only) | Drove the running app against §13 on Windows. **1** ✅ `npm start` boots + auto-opens the default browser (Chrome launched live; 4 deterministic tests assert `cmd /c start "" <url>` + the `CONCOURSE_NO_OPEN` escape hatch). **2** ✅ a read-3/write-1 turn showed reading→writing→done with correct friendly names (Reading Audience, Writing Summary, "Finished · 12s"); write landed on disk. **3** ✅ agent killed mid-turn → `blocked`, plain reason "The assistant stopped unexpectedly.", raw `exit code` behind a closed disclosure, transcript up to the failure preserved (nothing swallowed). **4** ✅ closed the tab + opened a fresh one → host replayed the full session (blocked rail + transcript + outcome + pinned file). **5** ✅ file written to `outputs/` pinned in the FileTray ("Just produced", New) sub-second and `/api/download` served it (Content-Disposition attachment). **6** ✅ `../`, absolute, encoded `../`, and download traversal all 403 across `/api/files`, `/api/file`, `/api/download`; escaping symlink/junction 403 (test 51). **8** ✅ every turn's events one-JSON-per-line, valid, append-only (`fs.appendFileSync`). **7** is the user's to arrange (real PM). No developer vocabulary observed anywhere in the UI; absolute paths never surfaced (friendly names only). |
| 2026-07-31 | E2E fix — reducer honesty: failed tool ≠ touched | host.mjs, test/reducer.test.mjs | Found during the §13 pass: a `Write` blocked by a permission check (TOOL_END `isError:true`) was still recorded in `filesTouched` as "Edited", so the outcome box claimed a write that never landed (soul.md honesty / §13-3 "nothing swallowed"). Fix: `TOOL_END` records the file only when `!evt.isError`; a blocked write / failed read is not shown as touched. The agent's own plain-language narration still explains the block. +2 reducer tests (blocked write, failed read → not recorded). |
| 2026-07-31 | E2E fix — concurrent-tool attribution (minimal) | host.mjs, test/reducer.test.mjs | Found during §13-2: Claude batches parallel Reads, and the reducer tracked one in-flight tool, so a concurrently-read file (`audience.md`) was silently dropped from the outcome summary while the current tool overwrote it. Minimal fix (user chose "patch now, clean look at it in v0.2"): added a per-turn `inFlight` map (toolUseId → {friendly,truePath,action}) populated on TOOL_START; TOOL_END attributes the file from that map so overlapping ends are all recorded, and a non-current tool's end records its file without resetting the rail (still shows the latest activity — full multi-activity rail is v0.2). Stray-end guard + ordered-stream fallback preserved. +1 test (interleaved concurrent reads → all attributed); live-verified the outcome box now lists Vision+Audience+Risks+Summary. |
| 2026-07-31 | E2E fix — §13-1 auto-open browser | host.mjs, test/open-browser.test.mjs | `npm start` now opens the default browser after `server.listen` (criterion 1: "no terminal interaction after that point"). Cross-platform `browserOpenCommand` (win32 `cmd /c start "" url` / darwin `open` / else `xdg-open`); `openBrowser` is best-effort (never throws on failure — startup must not depend on it) and suppressed by `CONCOURSE_NO_OPEN` (headless/tests). +4 tests (per-platform command, launch, suppression, failing-opener-doesn't-throw). |
| 2026-07-31 | 10 — persistence + reconnect replay | host.mjs, app.html, test/session-persistence.test.mjs | §7: after each turn `persistSession` writes `{sessionId,startedAt,workspaceRoot,turnCount}` to `<workspaceRoot>/.concourse/session.json` (best-effort, no-op until session_id). Capped (500) in-memory `eventLog` of the current turn's events, replayed to a (re)connecting client so a refresh restores the full transcript (not just headline state). On startup `loadPriorSession` reads session.json and offers "Pick up where you left off?" (banner, Continue/Start fresh) — never auto-resumed; Continue sets sessionId so the next turn passes `--resume`. 3 persistence tests; 49 green. Live-verified all three: session.json written; reload restored reasoning+steps+Finished; host restart showed the banner, Continue adopted the prior session id, and a follow-up recalled `outputs/recap.txt` with no file reads (real --resume). |
| 2026-07-31 | 9 — TranscriptSink | host.mjs, test/transcript-sink.test.mjs | Append-only JSONL audit (§10): every event (incl. SUBMIT with the prompt) recorded one JSON/line to `<workspaceRoot>/.concourse/transcripts/<sessionId>.jsonl`, wall-clock ISO timestamped, keyed by session_id, never rewritten/truncated. Pre-session events buffered then flushed in order once session_id is known. Synchronous writes (not lossy); a failed audit write is a **hard stop** — `record()` throws and `runTurn` halts the turn (interrupts engine, surfaces blocked "Couldn't save a record of this work") rather than swallowing. 3 tests (buffer+flush order+timestamps, append-only across turns, hard-stop throws); 43 green. Live-verified: a real turn wrote a 5-line transcript (SUBMIT→SESSION_OPEN→NARRATION×2→TURN_END/success), timestamps chronological; `.concourse/` gitignored. |
| 2026-07-30 | 1 — host.mjs skeleton | package.json, host.mjs, .gitignore | Host boots on 127.0.0.1:7317; `GET /health` returns ok + workspaceRoot + sessionId; ws `/session` sends idle-state hello on connect; importing host.mjs starts no server (pure pieces exported for later tests). |
| 2026-07-30 | 2 — CliEngine | host.mjs | CliEngine spawns `claude -p --output-format stream-json --verbose --include-partial-messages --allowedTools Read,Glob,Grep,Edit,Write --permission-mode acceptEdits` (no Bash, no dangerous-skip; `--resume` only when resuming), consumes NDJSON line-buffered into async-iterable RawEvents (with synthetic stderr/exit/spawn-error/parse-error events), captures session_id. Verified: real ws `submit` streamed 18 raw events to console (system/init, assistant, stream_event ×6, result/success, +misc subtypes) and pushed captured session_id to the client. |
| 2026-07-30 | 3 — EventNormaliser | host.mjs, fixtures/session-readwrite.ndjson, test/normaliser.test.mjs | `normaliseEvent` maps §5 table: system/init→SESSION_OPEN, assistant text→NARRATION, assistant tool_use→TOOL_START, user tool_result→TOOL_END, result→TURN_END (non-success surfaced, not swallowed), stream_event text_delta→partial NARRATION, synthetic exit/spawn→fatal HOST_ERROR, stderr/parse→non-fatal HOST_ERROR, __approval→APPROVAL_NEEDED (v0.2 seam). Dispatches on type, iterates blocks by type. 12 tests green, driven from a recorded real read+write session (105 lines). |
| 2026-07-31 | 8 — FileTray | host.mjs, app.html | host watches `outputs/` (fs.watch, debounced) and broadcasts `{type:"outputs-changed"}` so produced files surface without polling (§2.1). app.html: right-column FileTray consuming `/api/files` (folder browser with breadcrumbs, friendly names, dotfiles hidden) and `/api/download` (per-file link); files written to `outputs/` pinned in a "Just produced" section with a **New** badge. Two-column responsive shell (stacks under 900px). Verified live: tray listed workspace files with friendly names + downloads; a turn writing `outputs/greeting.txt` surfaced it pinned+New sub-second (well under 2s, criterion 5) and downloaded correctly (Content-Disposition attachment); zero console errors. 39 tests green. |
| 2026-07-30 | 7 — WorkspaceFs + REST | host.mjs, test/workspacefs.test.mjs | Scoped file surface (§11): WorkspaceFs resolves every client path against workspaceRoot, rejecting escapes both lexically (`../`, absolute, cross-drive) and via symlink/junction that resolves outside the real root → `WorkspaceError` 403. `list()` uses `lstat` and hides symlinks whose real target escapes the root; entries sorted by name. Endpoints `GET /api/files` (friendly entries), `/api/file` (text, 1 MB cap, X-Truncated/X-Size), `/api/download` (attachment). Tests green: direct containment + full endpoint integration incl. `../`, encoded traversal, absolute path, an escaping symlink→403, and an escaping link hidden from listings. The symlink tests use a directory-junction fallback and skip only where neither symlink nor junction can be created. Live smoke confirmed 403 on `../` and 200 + Content-Disposition on download. |
| 2026-07-30 | 6 — TurnTranscript | host.mjs, app.html, test/normaliser.test.mjs | host forwards each normalised event to browsers as `{type:"event"}`, enriched (TOOL_START→friendly name via §6.3 helper; TOOL_END→content flattened + capped at 4000 chars). Normaliser gains narration `channel` (speech vs thinking) + captures extended-thinking blocks/deltas as reasoning. app.html: scrolling transcript segment — reasoning streams live as prose (thinking channel styled distinctly with a blinking caret while open), tool steps collapse to one friendly line with raw detail behind a closed disclosure (never auto-expands), newest last with auto-scroll, resets per turn on new startedAt. Verified live: real turn streamed reasoning + Searching/Reading/Writing steps → Finished, zero console errors. 27 tests green. |
| 2026-07-30 | 5 — StatusRail | host.mjs, app.html, fonts/ (7 woff2) | host pipes each turn normaliseEvent→reduceState, broadcasts projected `{type:"state"}` to all clients (deduped, narration excluded so token deltas don't spam), holds turnState + replays it on (re)connect, serves /fonts same-origin. app.html: single-file React+htm from CDN, trainyard tokens inline, packaged Inter/Space Grotesk/JetBrains Mono, StatusRail with §6.1 state colours + pulse, live elapsed timer, Stop/interrupt, minimal composer, done/blocked outcome cards (friendly names, abs path in tooltip only, raw detail behind a closed disclosure). Verified live in browser: real read+write turn walked thinking→…→done ("Finished · 10s", Notes:Read + Summary:Edited), reload replayed done-state, zero console errors. 25 backend tests still green. |
| 2026-07-30 | 4 — StateReducer | host.mjs, test/reducer.test.mjs | `reduceState` implements the §6 machine (idle/thinking/reading/writing/running/awaiting_approval/blocked/done) with §6.2 tool→state map and §6.3 friendly-filename helper (q3_board_deck.pptx→"Q3 Board Deck", true path kept for tooltip). All surface text jargon-free. Failure paths covered: non-success TURN_END→blocked (plain reason, raw text in `detail`), fatal HOST_ERROR→blocked, non-fatal HOST_ERROR never overrides state. 12 tests green incl. a full fixture drive proving reading→writing→done with files-touched [Notes:read, Summary:edited]. Total suite: 24 tests green. |
