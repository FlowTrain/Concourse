# BUILD-LEDGER — Concourse

One row per completed unit of work (CLAUDE.md Rule 3).

| Date | Unit | Files touched | Acceptance criteria met |
|---|---|---|---|
| 2026-07-30 | 1 — host.mjs skeleton | package.json, host.mjs, .gitignore | Host boots on 127.0.0.1:7317; `GET /health` returns ok + workspaceRoot + sessionId; ws `/session` sends idle-state hello on connect; importing host.mjs starts no server (pure pieces exported for later tests). |
| 2026-07-30 | 2 — CliEngine | host.mjs | CliEngine spawns `claude -p --output-format stream-json --verbose --include-partial-messages --allowedTools Read,Glob,Grep,Edit,Write --permission-mode acceptEdits` (no Bash, no dangerous-skip; `--resume` only when resuming), consumes NDJSON line-buffered into async-iterable RawEvents (with synthetic stderr/exit/spawn-error/parse-error events), captures session_id. Verified: real ws `submit` streamed 18 raw events to console (system/init, assistant, stream_event ×6, result/success, +misc subtypes) and pushed captured session_id to the client. |
| 2026-07-30 | 3 — EventNormaliser | host.mjs, fixtures/session-readwrite.ndjson, test/normaliser.test.mjs | `normaliseEvent` maps §5 table: system/init→SESSION_OPEN, assistant text→NARRATION, assistant tool_use→TOOL_START, user tool_result→TOOL_END, result→TURN_END (non-success surfaced, not swallowed), stream_event text_delta→partial NARRATION, synthetic exit/spawn→fatal HOST_ERROR, stderr/parse→non-fatal HOST_ERROR, __approval→APPROVAL_NEEDED (v0.2 seam). Dispatches on type, iterates blocks by type. 12 tests green, driven from a recorded real read+write session (105 lines). |
| 2026-07-30 | 7 — WorkspaceFs + REST | host.mjs, test/workspacefs.test.mjs | Scoped file surface (§11): WorkspaceFs resolves every client path against workspaceRoot, rejecting escapes both lexically (`../`, absolute, cross-drive) and via symlink/junction that resolves outside the real root → `WorkspaceError` 403. Endpoints `GET /api/files` (friendly entries), `/api/file` (text, 1 MB cap, X-Truncated/X-Size), `/api/download` (attachment). 37 tests green (0 skipped): direct containment + full endpoint integration incl. `../`, encoded traversal, absolute path, and a real junction-escape → 403. Live smoke confirmed 403 on `../` and 200 + Content-Disposition on download. |
| 2026-07-30 | 6 — TurnTranscript | host.mjs, app.html, test/normaliser.test.mjs | host forwards each normalised event to browsers as `{type:"event"}`, enriched (TOOL_START→friendly name via §6.3 helper; TOOL_END→content flattened + capped at 4000 chars). Normaliser gains narration `channel` (speech vs thinking) + captures extended-thinking blocks/deltas as reasoning. app.html: scrolling transcript segment — reasoning streams live as prose (thinking channel styled distinctly with a blinking caret while open), tool steps collapse to one friendly line with raw detail behind a closed disclosure (never auto-expands), newest last with auto-scroll, resets per turn on new startedAt. Verified live: real turn streamed reasoning + Searching/Reading/Writing steps → Finished, zero console errors. 27 tests green. |
| 2026-07-30 | 5 — StatusRail | host.mjs, app.html, fonts/ (7 woff2) | host pipes each turn normaliseEvent→reduceState, broadcasts projected `{type:"state"}` to all clients (deduped, narration excluded so token deltas don't spam), holds turnState + replays it on (re)connect, serves /fonts same-origin. app.html: single-file React+htm from CDN, trainyard tokens inline, packaged Inter/Space Grotesk/JetBrains Mono, StatusRail with §6.1 state colours + pulse, live elapsed timer, Stop/interrupt, minimal composer, done/blocked outcome cards (friendly names, abs path in tooltip only, raw detail behind a closed disclosure). Verified live in browser: real read+write turn walked thinking→…→done ("Finished · 10s", Notes:Read + Summary:Edited), reload replayed done-state, zero console errors. 25 backend tests still green. |
| 2026-07-30 | 4 — StateReducer | host.mjs, test/reducer.test.mjs | `reduceState` implements the §6 machine (idle/thinking/reading/writing/running/awaiting_approval/blocked/done) with §6.2 tool→state map and §6.3 friendly-filename helper (q3_board_deck.pptx→"Q3 Board Deck", true path kept for tooltip). All surface text jargon-free. Failure paths covered: non-success TURN_END→blocked (plain reason, raw text in `detail`), fatal HOST_ERROR→blocked, non-fatal HOST_ERROR never overrides state. 12 tests green incl. a full fixture drive proving reading→writing→done with files-touched [Notes:read, Summary:edited]. Total suite: 24 tests green. |
